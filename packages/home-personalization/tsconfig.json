{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": [
    "Typechecking config: sources and tests together, no emit.",
    "Emit settings live in tsconfig.build.json rather than here, because",
    "a rootDir inherited through extends cannot be unset by a derived",
    "config, and workspace siblings resolved through paths sit outside it."
  ],
  "compilerOptions": {
    "target": "ES2021",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "isolatedModules": true,
    "strict": true,
    "noImplicitReturns": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": false,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["src/**/*.ts", "test/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
